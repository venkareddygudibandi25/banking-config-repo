spring:
  application:
    name: accounts-service

  profiles:
    active: dev

  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/accountsdb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Spsoft@630
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8070/eureka/
#   instance:
#     prefer-ip-address: true
